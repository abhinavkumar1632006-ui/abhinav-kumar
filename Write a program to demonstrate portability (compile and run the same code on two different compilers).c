#include <stdio.h>
int main() {
// Function to demonstrate portabilit {
   printf("This program demonstrates portability in C.\n");
    int a = 10, b = 20, sum;
    sum = a + b;
        printf("The sum of %d and %d is %d\n", a, b, sum);

    return 0; // Returning 0 indicates successful execution
}
